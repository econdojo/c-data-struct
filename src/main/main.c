#include <stdio.h>
#include <stdlib.h>
#include "example.h"

int main(int argc, char **argv) {
    graph();
    // tree();
    // queue();
    // stack();
    // list();
    // table();
    return 0;
}