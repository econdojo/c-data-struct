#ifndef MAIN_H
#define MAIN_H

#include "../graph/graph.h"
#include "../heap/heap.h"
#include "../list/list.h"
#include "../queue/queue.h"
#include "../stack/stack.h"
#include "../table/table.h"
#include "../tree/tree.h"

/* Graph */
int graph(void);

/* Tree */
int tree(void);

/* Queue */
int queue(void);

/* Stack */
int stack(void);

/* Linked list */
int list(void);

/* Hash table */
int table(void);

/* Heap */
int heap(void);

#endif